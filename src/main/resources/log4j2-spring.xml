<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">

    <Properties>
        <Property name="LOG_PATTERN">%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %c{1.} - %msg%n</Property>
    </Properties>

    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </Console>
    </Appenders>

    <Loggers>
        <!-- Your project logs -->
        <Logger name="com.revhire" level="INFO" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>

        <!-- Spring framework logs (keep INFO or WARN) -->
        <Logger name="org.springframework" level="INFO"/>

        <!-- SQL logs (optional: keep OFF for now) -->
        <Logger name="org.hibernate.SQL" level="OFF"/>
        <Logger name="org.hibernate.type.descriptor.sql" level="OFF"/>

        <Root level="INFO">
            <AppenderRef ref="Console"/>
        </Root>
    </Loggers>

</Configuration>
